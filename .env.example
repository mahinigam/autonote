# Environment variables template for autonote
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# Flask Configuration
# Generate a secure key using: python generate_env.py
# Or manually: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secure-secret-key-here-use-generate_env-script

# Environment setting
FLASK_ENV=development

# Google Gemini API Configuration
# Get your FREE API key from: https://aistudio.google.com/
# This is required for AI features to work
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Enable Flask debug mode for development
# FLASK_DEBUG=True

# Production deployment notes:
# - Use FLASK_ENV=production in production
# - Generate a longer secret key (64+ characters) for production
# - Set SECRET_KEY in your deployment platform's environment variables
# - Never use the same secret key across different environments
